# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on':
  pull_request:
    branches:
      - dev
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    env:
      VITE_RAPID_API_KEY: '${{ secrets.RAPID_API_KEY }}'
    steps:
      - uses: actions/checkout@v2
      - run: npm i -g firebase-tools
      - run: npm ci && npm run build
      - run: firebase use --add killoblanco-119f3 --token ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KILLOBLANCO_119F3 }}
      - run: firebase target:apply hosting open-weather open-weather
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_KILLOBLANCO_119F3 }}'
          target: open-weather
          projectId: killoblanco-119f3
